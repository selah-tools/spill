# Changesets

Create a changeset for any user-visible or release-worthy change:

```bash
pnpm changeset
```

The release workflow will open or update a release PR from the accumulated changesets on `main`.
